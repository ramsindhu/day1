
let array = "12345"   //its a strimg
let arry = []         //create new array a for storing the data
for(let i =1; i<=5;i++){      // its a syntax (condition) 
arry.push(array.substring(0,i)); // here substring chking like index so 0 then looping for i upto 5 so now the push function is adding the element in the last 
    
}
console.log(arry);
for (div of arry){       //for of loop - print the result nxt nxt line
    console.log(div);
}

let fruit = "banana"   // create a string
let count = fruit.split('a');
console.log(count);
   //create the new variable for split method, gvn the elmnt here wht v wnt to print d count  
console.log(count.length-1);  //new variable name.length for d result. length cosidered already 1 so put length-1 for actual count




